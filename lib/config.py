import os

env = {
    'ENV': 'production',
    'LOCAL_FOLDER': "", # parent folder of the project for loading local files
}
db = {
    'HOST': '', #Mongo Host
    'USERNAME': '', #Mongo User
    'PASSWORD': '' #Mongo Password
}
